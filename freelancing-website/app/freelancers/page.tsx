"use client";import{useEffect,useState}from"react";import Link from"next/link";import AppShell from"@/components/AppShell";import{api}from"@/lib/api";type F={_id:string;name:string;headline?:string;location?:string;rating:number;completedProjects:number;skills:string[]};export default function FreelancersPage(){const[people,setPeople]=useState<F[]>([]);const[search,setSearch]=useState("");const[skill,setSkill]=useState("All");const[loading,setLoading]=useState(true);async function load(){setLoading(true);const p=new URLSearchParams();if(search)p.set("search",search);if(skill!=="All")p.set("skill",skill);try{setPeople((await api<{freelancers:F[]}>(`/profile/freelancers?${p}`)).freelancers)}finally{setLoading(false)}}useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[search,skill]);const skills=["All","React","Next.js","Python","Cybersecurity","MongoDB"];return <AppShell><div className="mb-8"><p className="text-sm text-indigo-300">Talent marketplace</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Find people who can ship.</h1><p className="mt-2 text-sm text-white/35">Search real freelancer profiles from MongoDB.</p></div><div className="rounded-2xl border border-white/[0.08] bg-white/[0.025] p-4"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search name, skills, role or location..." className="h-12 w-full rounded-xl border border-white/[0.08] bg-black/20 px-4 text-sm outline-none focus:border-indigo-400/50"/><div className="mt-4 flex gap-2 overflow-x-auto">{skills.map(s=><button key={s} onClick={()=>setSkill(s)} className={`whitespace-nowrap rounded-lg px-3 py-2 text-xs ${skill===s?"bg-white text-black":"bg-white/[0.04] text-white/40"}`}>{s}</button>)}</div></div>{loading?<div className="mt-6 rounded-2xl border border-white/[0.07] p-10 text-center text-sm text-white/30">Loading talent...</div>:people.length===0?<div className="mt-6 rounded-2xl border border-white/[0.07] p-10 text-center text-sm text-white/30">No matching freelancers.</div>:<div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{people.map(p=><article key={p._id} className="rounded-2xl border border-white/[0.08] bg-white/[0.025] p-5"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-gradient-to-br from-indigo-400 to-sky-400 text-sm font-bold">{p.name.split(" ").map(n=>n[0]).join("")}</div><h2 className="mt-4 font-medium">{p.name}</h2><p className="mt-1 text-xs text-white/35">{p.headline||"Freelancer"} · {p.location||"India"}</p><div className="mt-4 flex gap-4 text-xs text-white/45"><span>★ {p.rating||"New"}</span><span>{p.completedProjects} projects</span></div><div className="mt-4 flex flex-wrap gap-2">{p.skills.slice(0,6).map(s=><span key={s} className="rounded-lg bg-white/[0.05] px-2.5 py-1 text-[11px] text-white/50">{s}</span>)}</div><Link href={`/freelancers/${p._id}`} className="mt-5 block w-full rounded-xl border border-white/[0.09] py-2.5 text-center text-xs font-medium text-white/60 hover:bg-white hover:text-black">View profile</Link></article>)}</div>}</AppShell>}
